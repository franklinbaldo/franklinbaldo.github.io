name: FlyDoom motor truth UI polish

on:
  push:
    branches:
      - feature/flydoom-motor-telemetry
    paths:
      - scripts/polish_flydoom_motor_truth_ui.py

permissions:
  contents: write

jobs:
  polish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: feature/flydoom-motor-telemetry
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Apply UI polish
        run: python scripts/polish_flydoom_motor_truth_ui.py
      - name: Format page
        run: npx prettier --write public/flydoom/index.html
      - name: Commit polish
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/flydoom/index.html
          git diff --cached --check
          git commit -m "feat(flydoom): polish motor truth instrument panel" || exit 0
          git push origin HEAD:feature/flydoom-motor-telemetry
