name: MaleCNS Directed Peer Gains Kaggle

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: malecns-directed-peer-gains-kaggle
  cancel-in-progress: true

jobs:
  run-directed-peer-gains:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled while semantic-cache Kaggle benchmark is the only active MaleCNS submission."
