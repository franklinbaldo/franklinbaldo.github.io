---
date: "2026-09-05T05:06:26Z"
branch: ux-ui/run-2026-09-05T05-00-33
status: open
---

# Sessão 2026-09-05T05-06-26 — UX/UI

Issues fechadas: #1676
O que foi feito: substituído slug bruto de perspectiva (`id.replace(/-/g, " ")`)
pelo nome de exibição vindo de `getPerspectives()` em 6 pontos reader-facing:
chip de filtro e tag do card em `battles/index.astro`, meta description e tag
em `battles/[id].astro`, link de histórico em `posts/[key].astro`, e
`formatPerspective` em `RankingView.astro` (agora construído a partir do
prop `perspectivesGrid`). `data-perspective`/`href`/`style` continuam
usando o id bruto; fallback para `id.replace(/-/g, " ")` mantido para ids
órfãos sem entrada em `getPerspectives()`.
CI local: astro check ✅ (0 erros) · prettier ✅ · build ✅ (conferido no HTML
gerado de `/ranking/`, `/ranking/battles/`, `/ranking/battles/<id>/` e
`/ranking/posts/<key>/`, EN e PT).
