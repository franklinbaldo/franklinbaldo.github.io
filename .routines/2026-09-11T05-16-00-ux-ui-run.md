---
date: "2026-09-11T05:16:00Z"
branch: ux-ui/run-2026-09-11T05-01-17
status: open
---

# Sessão 2026-09-11T05-16-00 — UX/UI

Issues fechadas: #1705, #1706, #1707
O que foi feito:
- #1705: adicionadas dimensões `width`/`height` explícitas nos quatro
  `<img>` de meme/logo do PowerPoint Club (Theranos, memegen astronaut/right/
  spiderman) em `src/pages/powerpoint-club/nathan-holmes.astro`, evitando CLS —
  valores obtidos baixando cada imagem e lendo as dimensões reais.
- #1706: `RankingView.astro` e `ranking/posts/[key].astro` agora formatam
  μ/σ/ordinal com `toLocaleString(locale, ...)` (mesma variável `locale` já
  usada para datas) em vez de `.toFixed()`, para usar vírgula decimal em PT.
- #1707: adicionado `#factory-filter-status[role=status][aria-live=polite]`
  (visualmente oculto via `.sr-only`) no Repo Factory, atualizado em
  `updateVisibility()` com a contagem de resultados visíveis; busca por texto
  agora tem debounce de 300ms para não anunciar por tecla.

Também mesclado (passo 0 da rotina): PR #1700 estava com a branch atrasada em
relação a main (`mergeable_state: behind`), bloqueando o required check
"GitGuardian Security Checks"; atualizada via
`mcp__github__update_pull_request_branch`, CI ficou verde, mesclada com squash.

CI local: astro check ✅ (0 erros, 0 warnings) · prettier ✅ · build ✅ (4011 páginas)
