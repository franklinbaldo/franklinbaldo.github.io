---
date: "2026-09-09T05:12:02Z"
branch: claude/ecstatic-hawking-09khwb
status: open
---

# Sessão 2026-09-09T05-12-02 — UX/UI

Passo 0 da rotina: PR #1692 ("ux: localize per-duel date in battle archive,
document i18n decision") tinha o job `check` falhando por um erro
pré-existente em `src/pages/audio/index.astro` (documentado no PR), já
corrigido no `main` por #1694 desde então. Atualizei a branch do PR com
`update_pull_request_branch`, CI ficou 100% verde, mesclado com squash —
fechou #1689 e #1083.

Backlog ficou vazio (0 issues `routine` abertas) após o merge acima, bem
abaixo do piso de ~10 do passo 1. Rodei uma auditoria (home, blog post,
`/archive/`, `/ranking/`, Header, GlobalMusicPlayer) e abri duas novas
issues (#1698, #1699). Um quarto achado (sub-páginas de `/ranking/battles`
e `/ranking/perspectives` sem versão `/pt/`) parece decisão de escopo
deliberada (como `/changelog/`), não uma issue clara — não abri, deixo para
decisão humana.

Implementei o item mais claro e verificável achado na auditoria (ainda sem
issue própria, corrigido direto por ser trivial e de escopo mínimo):
`src/pages/404.astro` e `src/pages/pt/404.astro` tinham
`<pagefind-searchbox>` sem `data-filter`, ao contrário de
`search.astro`/`pt/search.astro` (que já usam `data-filter="lang:en"` /
`"lang:pt"`) — um leitor que caía numa página 404 (justo o momento em que
mais precisa da busca) via resultados misturados EN+PT, enquanto `/search/`
já filtrava por idioma. Adicionado `data-filter="lang:en"`/`"lang:pt"` aos
dois `<pagefind-searchbox>`, confirmado no HTML gerado
(`dist/404.html`, `dist/pt/404/index.html`).

CI local: astro check ✅ (0 erros) · prettier ✅ · build ✅ (4004 páginas)
