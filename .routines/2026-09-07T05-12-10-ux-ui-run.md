---
date: "2026-09-07T05:12:10Z"
branch: claude/ecstatic-hawking-g0mzq1
status: open
---

# Sessão 2026-09-07T05-12-10 — UX/UI

Issues fechadas: #1684, #1685, #1686

O que foi feito:

- #1684: contador visível `X songs`/`X músicas` (`role="status" aria-live="polite"`)
  ao lado dos chips de gênero em `src/pages/music.astro` e
  `src/pages/pt/musicas.astro`, atualizado no listener de clique do chip
  container junto com `card.hidden`.
- #1685: `GlobalMusicPlayer.astro` ganhou um `<span role="status"
  aria-live="polite" class="sr-only">` dedicado (`#gmp-now-playing`),
  atualizado em `showId()` com o dicionário `L.nowPlaying` (EN/PT) exposto via
  `data-now-playing` no elemento `#gmp`.
- #1686: `ShareButton.astro` ganhou um `<span class="sr-only share-status"
  role="status" aria-live="polite">` irmão do botão, atualizado em `flash()`
  junto com o texto visível do botão — leitores de tela agora recebem
  "Link copiado"/"Link copied" (ou o erro) sem depender de `aria-label`
  estático.

CI local: astro check ✅ (0 erros novos; os 3 erros pré-existentes em
`src/pages/audio/index.astro` não têm relação com este diff) · prettier ✅ ·
build ✅ (2x, incl. prebuild/pregen e pagefind).

Nota: o build deste sandbox não tem acesso de rede à Suno API (`music.astro`/
`pt/musicas.astro` renderizam o banner de erro e `showGenreChips` fica
`false`), então a verificação do contador de gênero via `dist/**/index.html`
não foi possível aqui — validado em vez disso com um harness Playwright
isolado reproduzindo a mesma lógica de filtro (3→2→3 ao alternar chip).
`GlobalMusicPlayer` e `ShareButton` foram confirmados via grep em
`dist/blog/*/index.html` e `dist/pt/blog/*/index.html` (EN e PT).
