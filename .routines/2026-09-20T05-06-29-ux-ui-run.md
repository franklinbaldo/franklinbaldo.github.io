---
date: "2026-09-20T05:06:30Z"
branch: ux-ui/run-2026-09-20T05-01-45
status: open
---

# Sessão 2026-09-20T05-06-29 — UX/UI

Issues fechadas: #1915

O que foi feito:

- Backlog de issues `routine` estava zerado (0 abertas) no início da sessão —
  todas as pendentes de sessões anteriores (#1749, #1750, #1751 etc.) já
  haviam sido fechadas. Fiz auditoria manual seguindo as fontes da seção 6
  (padrões de i18n numérico já identificados em sessões anteriores) e
  encontrei um caso não coberto: o tooltip do rank-badge do archive ainda
  usava `.toFixed(2)` no ordinal, ignorando o locale pt-BR — bug irmão do já
  corrigido em #1736 (que tratou só o `href`, não o tooltip, como a própria
  issue já registrava). Abri #1915 para documentar e resolvi na mesma sessão.
- #1915 — `rankInfo.row.ordinal.toFixed(2)` trocado por
  `toLocaleString(locale, { minimumFractionDigits: 2, maximumFractionDigits: 2 })`
  em `archive/index.astro`, `archive/page/[n].astro` (locale `en-US`) e
  `pt/archive/index.astro`, `pt/archive/page/[n].astro` (locale `pt-BR`).

Verificação em build real (grep no `dist/`):

- `dist/pt/archive/index.html` e `dist/pt/archive/page/2/index.html`:
  tooltip `rank-badge` → `(ordinal 5,27)` (vírgula).
- `dist/archive/index.html` e `dist/archive/page/2/index.html`: tooltip
  inalterado, `(ordinal 5.27)` (ponto).

CI local: astro check ✅ (0 errors, warnings/hints pré-existentes) · prettier
✅ (nenhum dos 4 arquivos tocados aparece nos avisos pré-existentes) · build
✅ (4480 páginas HTML, pagefind ok).
