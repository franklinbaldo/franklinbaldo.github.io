---
date: 2026-06-29T14:00:00
slug: lighthouse-ci
branch: claude/sleepy-pasteur-ttwf39
status: pr-open
issues: [764]
pr_opened: null
pr_merged: null
---

## Contexto ao chegar

13 issues `routine` abertas — backlog dentro da faixa saudável (10–20). PR #806 (`ux: related posts enhancements`) estava com CI vermelho por falha de check:hygiene: três arquivos de scratch (`decide_args.txt`, `decide_match1.sh`, `decide_match3.sh`) e um journal mal nomeado (`.routines/2026-06-25T18-15-00Z-hronir-run.md` com `Z` trailing) bloqueando o merge.

## O que mergeou

Nada — CI vermelho no PR #806. Adicionei commit de remediação à branch `claude/sleepy-pasteur-fsgb5n` removendo os scratch files e renomeando o journal com `Z` trailing. A run seguinte pode mergear o #806 se CI ficar verde.

## O que fez e por quê

Trabalhou na issue #764 (Lighthouse CI no workflow de check):

1. **`.lighthouserc.cjs`** — config usando `staticDistDir: './dist'` (sem servidor externo, lhci sobe seu próprio servidor a partir do build estático). Assertiva de performance em modo `warn` (minScore: 0.7), preset `lighthouse:no-pwa`. `numberOfRuns: 1` para manter custo de CI baixo.

2. **`check.yml`** — passo `Lighthouse CI` adicionado após `Build`, com `continue-on-error: true` como safety extra além do modo `warn` nativo do lhci. Não bloqueia CI nesta fase de calibração de baseline.

3. **`scripts/check-hygiene.mjs`** — `.lighthouserc.cjs` adicionado ao `ROOT_ALLOWLIST` para não bloquear o hygiene check.

4. **Hygiene remediation** — removidos `decide_args.txt`, `decide_match1.sh`, `decide_match3.sh` (scratch files do hronir) e renomeados dois journals com nome inválido (`.routines/2026-06-25T18-15-00Z-hronir-run.md` → sem `Z`; `.routines/run-2026-06-27T13-04-00-hronir-run.md` → sem prefixo `run-`).

5. **`@lhci/cli`** instalado como devDependency.

Build local passou; `check:hygiene` e `astro check` verdes.

## O que ficou pra próxima

- Mergear PR #806 se CI ficar verde após o fix de hygiene.
- Mergear este PR (o PR de Lighthouse CI) após veto window de 24h.
- Após alguns PRs mergeados com Lighthouse, avaliar se é hora de tornar a assertiva bloqueante (threshold `error` em vez de `warn`).
