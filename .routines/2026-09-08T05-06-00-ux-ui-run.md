---
date: "2026-09-08T05:06:00Z"
branch: claude/ecstatic-hawking-x2wqz4
status: open
---

# Sessão 2026-09-08T05-06-00 — UX/UI

Issues fechadas: #1689, #1083

O que foi feito:

- `src/pages/ranking/battles/index.astro`: `fmtDate` estava sempre em
  `en-US`, mesmo para duelos entre dois posts PT — inconsistente com
  `battles/[id].astro`, que já resolve a data no idioma do duelo. Adicionado
  `duelLang(d)` (mesma regra de precedência do RFC 0012 §6 regra 4: só é
  "pt" quando vencedor e perdedor são ambos "pt") e `fmtDate` passou a
  aceitar o idioma como parâmetro.
- `battles/index.astro`, `perspectives/index.astro`, `perspectives/[id].astro`:
  documentada no código a decisão de manter o idioma estrutural da página
  (breadcrumbs, headings, `HronirExplainer`) em inglês — são páginas de
  índice/agregação que listam itens de idiomas mistos (duelos, líderes de
  perspectiva, posts ranqueados), sem um único "idioma da página" para
  alternar, diferente de `battles/[id].astro` que resolve isso por ter
  exatamente um duelo. A localização por item (título do post, e agora
  também a data) já existia ou foi adicionada, e continua funcionando.
- #1083 pedia exatamente essa decisão documentada; seu escopo restante
  (arquivos que ainda existem) é coberto integralmente por este PR, e os
  arquivos que citava (`version-trials/index.astro`, `versions/[key].astro`)
  não existem mais no repo — fechada como superada por #1689.

CI local: astro check ✅ (3 erros pré-existentes em `audio/index.astro` e
`blog/[...slug].astro`, não relacionados a esta mudança — confirmado com
`git stash`) · prettier ✅ · build ✅ (verificado no HTML gerado: datas de
duelos EN continuam en-US; nenhum duelo pt-pt existe hoje nos dados, então o
novo branch pt-BR é código morto até que exista um, mas corrige um bug
latente e segue o precedente do repo).
