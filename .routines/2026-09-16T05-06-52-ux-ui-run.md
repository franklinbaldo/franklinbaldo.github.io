---
date: "2026-09-16T05:06:52Z"
branch: ux-ui/run-2026-09-16T05-00-03
status: open
---

# Sessão 2026-09-16T05-06-52 — UX/UI

Issues fechadas: #1735, #1736, #1737

O que foi feito:

- #1735 — números de avaliação (`avgDuels`, cards de recent battles em
  `RankingView.astro`, `.duel-rates` em `ranking/posts/[key].astro`,
  `.versus-rate` em `ranking/battles/[id].astro`, `.hronir-rate` em
  `HronirReviews.astro`) agora usam `toLocaleString(locale, ...)` em vez de
  `.toFixed(n)`, exibindo `,` como separador decimal em contexto PT. `fmtNum`
  em `RankingView.astro` ganhou um segundo parâmetro `digits` (default 2)
  para cobrir também os pontos de 1 casa decimal.
- #1736 — badge de rank do archive (`archive/index.astro`,
  `archive/page/[n].astro`, `pt/archive/index.astro`,
  `pt/archive/page/[n].astro`) agora aponta para
  `/ranking/posts/<translationKey>/` (dossiê do post) em vez do `/ranking/`
  genérico, alinhado ao padrão já usado em `BlogPostPage.astro`.
- #1737 — `SeriesContext.astro` (variant `aside`) agora envolve o post atual
  num `<span aria-current="page">`, seguindo o padrão de `Breadcrumbs.astro`.
  Texto `(this post)`/`(este post)` mantido.

Verificação em build real (grep no `dist/`):

- `dist/pt/blog/<slug>/index.html`: badge `.hronir-rate` → `4,8★` (vírgula).
- `dist/pt/ranking/index.html`: `avgDuels` → `53,6`; `.bcl-rates` →
  `3,9 · 2,6` (vírgula).
- `dist/archive/index.html` e `dist/pt/archive/index.html`: `.rank-badge`
  aponta para `/ranking/posts/<key>/`.
- `dist/blog/<slug com série>/index.html`: `<span aria-current="page">`
  presente no post atual da lista da série.
- `.duel-rates` (`ranking/posts/[key]`) e `.versus-rate`
  (`ranking/battles/[id]`) verificados em EN (saída idêntica, `.`
  preservado); não há hoje nenhuma duel/battle cujo `translationKey`
  resolva para `lang="pt"` no corpus atual (todo post PT com histórico de
  duelo tem contraparte EN, que vence por regra), então o caminho PT desses
  dois pontos específicos não pôde ser observado no build atual — a lógica é
  simétrica à dos outros pontos já verificados no mesmo arquivo (`fmtDate`/
  `fmtOrdinal` reusam a mesma `locale`).

CI local: astro check ✅ (0 errors, warnings pré-existentes) · prettier ✅ ·
build ✅ (4051 páginas, pagefind ok).
