---
date: 2026-06-15T05:00:00
slug: law-and-ai-path
branch: claude/sleepy-pasteur-k04fua
status: pr-open
issues: [242]
pr_opened: null
pr_merged: null
---

## Contexto ao chegar

Run autônoma de 2026-06-15. Nenhum PR com label `routine` estava aberto — o PR anterior da run
de 2026-06-14 (lazy images perf, issue #492) não apareceu na listagem de abertos, o que indica
que foi mergeado (issue #492 também não está mais na lista de abertas).

Backlog com 9 issues `routine` abertas — abaixo do mínimo de 10. Situação exige reposição antes
de executar.

## Reposição do backlog

Criei 4 novas issues pensando no futuro do blog:

- **#550** — ux: prev/next navegação de série ao final de posts com `series` (priority:media)
  Rationale: a série harness tem 5+ posts mas nenhuma navegação entre eles ao final de cada post.
- **#551** — seo: meta description audit para posts sem description ou com description genérica (priority:media)
  Rationale: descriptions ausentes ou fracas reduzem CTR orgânico.
- **#552** — a11y: focus-visible styles — audit completo dos interativos (priority:baixa)
  Rationale: overrides de outline em componentes customizados podem suprimir indicadores de foco.
- **#553** — perf: audit e fix de dimensões explícitas em imagens para prevenir CLS (priority:baixa)
  Rationale: complementa o lazy loading da run anterior; imagens sem width/height causam CLS.

Backlog agora em 13 issues (dentro da faixa 10–20).

## O que fiz

Implementei issue **#242 — ux: novo reading path "Direito e IA" — trilha de entrada para advogados**.

Auditoria prévia:
- Confirmei que todos os posts candidatos existem em `versions-selected.json` e não têm `draft: true`
- Mapeei os pares EN/PT via `translationKey`:
  - `the-art-of-delegation` / `delegando-para-agentes` (tk: `delegating-to-agents`)
  - `census-not-sample` / `censo-nao-amostra` (tk: `census-not-sample`)
  - `conceptual-document-the-chronicle-of-franklin-baldo` / `documento-conceitual-a-cronica-de-franklin-baldo` (tk: `conceptual-document`)
  - `building-funes` / `construindo-funes-como-dei-uma-alma-a-um-agente-de-ia` (tk: `building-funes`)
  - `funes-soul` / `soulmd-funes` (tk: `funes-soul`)

Ordem de leitura escolhida (do mais familiar ao mais profundo):
1. Delegação (metáfora jurídica → delegação para agentes de IA)
2. Economia de IA pelo olhar de fiscal de tributos
3. Blueprint do sistema de documentação intelectual
4. Construindo o agente de memória
5. A questão filosófica da identidade/alma do agente

Mudanças:
- `src/lib/paths.ts`: novo `ReadingPath` com slug `law-and-ai`, título bilíngue, blurb bilíngue, 5 posts EN + 5 PT
- `src/components/PathsTeaser.astro`: ícone SVG thin-line de balança (estilo cobogó) — beam horizontal, dois pans circulares, haste vertical + base, fulcro com `fill="currentColor"`

Checks locais:
- `npm run build`: 2898 HTML gerados, 0 erros ✓
- `npx astro check`: 0 errors, 0 warnings ✓
- `npx prettier --check .`: All matched files use Prettier code style ✓

Os artefatos gerados (`versions-selected.json`, `ranking-snapshot.json`) foram atualizados durante o
prebuild (hronir:select escolheu versões mais novas de alguns posts) e commitados junto.

## Inspeção visual a fazer na próxima run

Como a checagem de pixel fica para pós-publicação, a próxima run deve verificar:
- `/paths/` (EN) — nova card "Law and AI" aparece, ícone de balança renderiza, blurb correto
- `/pt/paths/` (PT) — "Direito e IA" aparece, mesmo ícone
- `/paths/law-and-ai/` e `/pt/paths/law-and-ai/` — lista os 5 posts corretos em cada língua

## O que ficou para a próxima run

- Mergear este PR se CI verde e sem comentário bloqueante de Franklin
- Verificar screenshot de prod das páginas `/paths/` EN e PT após deploy
- Continuar com issues de prioridade média: #550 (series nav), #551 (meta desc audit), #493 (TOC scroll spy)
