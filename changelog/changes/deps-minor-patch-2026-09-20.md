---
type: changelog
date: 2026-09-20
description: Refresh low-risk JavaScript dependencies with current patch and minor fixes for Markdown parsing, linting, formatting, and image metadata handling.
tags: [dependencies, maintenance, markdown, lint, performance]
---

# Minor and patch dependency refresh

Updates `image-size` to 2.0.4, `marked` to 18.0.13, `oxlint` to 1.83.0, and `prettier` to 3.9.8.

The refresh stays within the existing major versions and is intended to pick up upstream bug fixes and small performance improvements while keeping the repository's current dependency architecture unchanged.
