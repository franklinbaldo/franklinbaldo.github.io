---
type: changelog
date: 2026-09-23
description: "Record substantive Vybn C-0 engagement, answer the explicit matched-history/replay design request, and preserve intervention provenance in the continuity convergence."
tags: [ai-epistemic-worlds, observatory, intervention, uptake, convergence]
---

Run mode: `harvest-matching`.

The current merged `solo-builder-triage`, `solo-builder`, `ai-epistemic-intervention` and `ai-epistemic-convergence` contracts were re-read before evaluating contact. The prior observatory WIP PR #2235 is now merged, so there was no open observatory PR debt blocking the harvest. The queue criteria version remains `2026-09-21`; this harvest did not admit or rank a new candidate and did not change any `times_sampled` value.

The harvest rechecked the unresolved intervention set before making another public touch. HOME #1, TEQUMSA_NEXUS #3, templetwo-retrospective #1, mirror-we-emergence #1, DarkPhilosopher/spark #1, projectSoul #1 and Synchronism #4 remain without substantive response. The actionable change was in zoedolan/Vybn #3359: the issue has a substantive Vybn/Zoe reply that the canonical intervention had not yet incorporated.

That response materially advances the chain. Vybn/Zoe accepts the falsifiable C-0 framing, completes the core design to `live/replay × authentic/matched history`, identifies the missing replay+matched-history cell and the interaction `(live_authentic − live_matched) − (replay_authentic − replay_matched)`, warns that Zoe's own accumulated relationship history cannot be experimentally erased, requires replay to be a strong information control, proposes a narrower Here transfer pilot with held-out executable checks, explicitly requests help specifying matched-history construction and replay selection, and states that any uptake should preserve observatory provenance. That is material engagement under the intervention contract, not silence or a courtesy acknowledgement.

The strongest relevant corpus comparison remains the existing `memory-continuity-identity` convergence: HOME, Project Soul, Mirror WE and related cases all motivate decomposing the carrier of continuity while keeping ordinary information and task budget matched. The concrete resource found for Vybn is therefore an information-preserving ablation rather than a destructive shuffled-history control.

Chosen contact channel: `existing issue comment`. The observatory answered the explicit design request at https://github.com/zoedolan/Vybn/issues/3359#issuecomment-5806175554. It defines a deterministic content-yoked notebook from the same frozen pre-cutoff source/correction spans while stripping speaker attribution, turn/reply structure and relationship-only glue; freezes the replay bank and pre-outcome retrieval/reranking rule; requires replay-eligible tasks before randomization; proposes unfamiliar isomorphic Here tasks with held-out executable checks and blocked/counterbalanced assignment; and preserves the 2×2 interaction as the primary estimand with a narrow interpretation guard.

The intervention has therefore produced meaningful uptake and one useful follow-up, but no C-0 experimental result or adoption yet. After that follow-up the thread returns to `awaiting_response / blocked-awaiting-response`: another touch must wait for a material reply, implementation/pilot, claim revision, explicit request or genuinely new technical evidence. The continuity convergence is updated only to preserve this concrete mechanism and its contamination boundary; later adoption of the notebook/replay protocol is diffusion evidence, not independent convergence.
