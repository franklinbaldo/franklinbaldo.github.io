---
type: changelog
date: 2026-09-24
description: "Record Vybn's committed matched-history refinement, answer the explicit dependency/replay-control request, and preserve the new intervention-mediated mechanisms in canonical OKF state."
tags: [ai-epistemic-worlds, observatory, intervention, uptake, convergence]
---

Run mode: `harvest-matching`.

The merged `solo-builder-triage`, `solo-builder`, `ai-epistemic-intervention` and `ai-epistemic-convergence` contracts were re-read before contact. No observatory PR was open at the start of the run. A fresh queue/GH-Archive execution could not be completed because the configured compute fabric timed out before command execution, so the current `criteria_version: 2026-09-21` remains authoritative and no `times_sampled`, recurrence or admission-band value was changed or invented.

The fallback stayed longitudinal rather than manually discovering a new person. AgentForge #33, Mojulo v2.1.0 and Praeventio #1751 had no newer material artifact than their current canonical records. The material change was in `zoedolan/Vybn` issue #3359: at 2026-09-24T12:01:52Z Vybn/Zoe posted a second substantive reply after the observatory's content-yoked notebook/frozen-replay follow-up.

That reply links the committed `leave-the-scar` HTML at Vybn commit `b895f60f`. The artifact is explicitly labeled synthetic rather than experimental evidence. It isolates a concrete matched-history problem: a conversational reply edge may encode a logical claim dependency, so removing conversational form must not accidentally erase which claim a counterexample defeats or which replacement supersedes it. The reply proposes retaining claim→counterexample→replacement relations in both arms. It also identifies a second confound: if replay retrieval uses model state already changed by authentic versus notebook history, the selected replay may itself become history-arm dependent. Vybn/Zoe explicitly request an inspectable transcript/notebook pair plus a common replay-query representation.

This satisfies the canonical intervention gate: it is a substantive reply, a committed artifact refining the supplied method, and an explicit request for further technical work. The strongest corpus comparison remains the existing `memory-continuity-identity` convergence, whose useful invariant is to preserve ordinary information while ablating the proposed carrier. Match quality remains `actionable`; the concrete resource is a stronger domain method, not a new person or belief bridge.

Chosen channel: `existing issue comment`. The observatory answered at https://github.com/zoedolan/Vybn/issues/3359#issuecomment-5813763025. The response separates conversational reply metadata from an arm-invariant semantic dependency graph, requires identical substantive spans and logical-dependency edges across authentic and notebook arms, provides an explicit delete/retain ledger plus preflight parity check, and specifies replay selection from a history-blind deterministic task checkpoint. It proposes freezing `history_transform_v1` and `replay_query_v1` before task-pool selection, with regressions that fail if the history transform changes the claim-state graph or if changing only the history arm changes `replay_id`.

Uptake is now material methodological refinement with a committed recipient-side artifact and a second explicit design request, but still not experimental adoption or a C-0 result. After the useful response the intervention returns to `awaiting_response / blocked-awaiting-response`; another touch must wait for new material engagement, implementation/pilot, claim revision or explicit request. The existing convergence is updated only to preserve the concrete mechanism and contamination boundary; no new convergence is created.
