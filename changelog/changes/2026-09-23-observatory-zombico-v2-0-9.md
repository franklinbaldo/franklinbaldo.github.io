---
type: changelog
date: 2026-09-23
description: "Update the Mojulo solo-builder record after v2.0.9 turns an artifact-host CSP failure into explicit CDN/offline export behavior while the unresolved resource remains an independent cold-start user."
tags: [ai-epistemic-worlds, solo-builder, observatory]
---

Run mode: `discovery`.

The merged `solo-builder-triage`, `solo-builder`, `ai-epistemic-intervention` and `ai-epistemic-convergence` contracts were read before selection or contact. The canonical queue refresh was attempted under criteria version `2026-09-21`, but the connected compute fabric timed out before GH Archive sampling. `times_sampled` therefore remains 1 and no recurrence claim was created; the pass used the documented fallback to an already investigated card with material new evidence.

The longitudinal review of `zombico/mojulo` found a material v2.0.9 change. A field failure showed that the artifact host rejects inline `data:` scripts through CSP even when a page is comfortably below the nominal size limit. Mojulo now makes normal web exports CDN-first, preserves a separately named offline page, consumes the host profile's declared CDN capability in the handoff logic, and keeps downloadable bundles self-contained. This further reduces host-portability uncertainty without creating independent product reception.

The blocker remains `missing_resource: user`, subtype `cold-start-tester`. Accumulated primary-repository reception was refreshed at 3 stars, 0 forks, 3 watchers, 0 subscribers, 0 external issues and 0 external pull requests; discussion participation and distinct external contributors remain not measured. Full relevant-corpus comparison keeps the best bridge only `plausible`: GodOS/Scrolls/FlameGrid and EpiLogos are possible world/simulation consumers without a concrete public Mojulo-shaped task, while Spark, Medieval Economy and ISU Survivor currently need field validation, methodology review and benchmark instrumentation respectively. No resource was found that closes the user gap, no convergence was created, and no public contact was made; the account-level initial-touch gate remains unused.
