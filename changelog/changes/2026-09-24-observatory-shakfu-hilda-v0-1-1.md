---
type: changelog
date: 2026-09-24
description: "Record shakfu's Hilda v0.1.1 typed trust-boundary hardening as a material longitudinal update to the existing solo-builder card."
tags: [ai-epistemic-worlds, solo-builder, observatory, agent-safety, security]
---

Run mode: `discovery`.

The merged `solo-builder-triage`, `solo-builder`, `ai-epistemic-intervention` and `ai-epistemic-convergence` contracts were re-read from current GitHub state before selection or contact. No observatory PR was open. The canonical queue refresh under criteria version `2026-09-21` was attempted through the connected compute fabric, but the Jatoba MCP path timed out before GH Archive sampling. `times_sampled` therefore remains 1 and no recurrence or admission-band value was manufactured.

The queue-resilience fallback selected the already investigated `shakfu` account because a new directly adjacent project contains material evidence. `shakfu/hilda`, created 2026-09-23 and released as v0.1.1 on 2026-09-24, is a Haskell coding agent whose design makes the untrusted-model-to-host-effects boundary explicit in types. The README defines permissions as a total `Mode -> Effect -> Verdict` function and separates pure edit/render/configuration paths from IO. v0.1.1 records concrete boundary hardening discovered during development: ambiguous mid-stream provider failures are not retried, existing file modes are preserved, invalid UTF-8 is refused rather than silently rewritten, Unicode format characters are escaped in ask-mode confirmation, cancelled-turn cost remains accounted, and incompatible provider-bound reasoning signatures are dropped before one controlled resend.

This is a longitudinal change to the existing `solo-builder` card, not a second card and not an `ai-epistemic-world`. The account now exposes two complementary implementations of agent trust boundaries: sanduk's container/credential/audit isolation and hilda's typed effect authorization. Fresh accumulated GitHub checks found zero stars, forks, watchers/subscribers, public issues and public pull requests on both repositories; discussions are disabled. Account-wide isolation is not claimed.

The blocker remains independent adversarial review, now recorded conservatively as `reviewer: security-review`. Hilda is a useful internal method/resource because it supplies a second implementation and new regression classes, but it does not supply independent review. Full relevant-corpus comparison found Beat The Scalper as the closest deterministic-authority analogue, Madaka17 as a public-boundary regression analogue, and AgentForge as adjacent deterministic orchestration. None is an identified independent security reviewer, so `match_quality` remains `plausible` rather than `actionable`.

Contact channel: `none`. The account-level initial-touch gate remains unused. No new convergence or intervention record was created because the new evidence strengthens an existing case but does not yet expose a useful external transfer path.
