---
type: changelog
date: 2026-09-24
description: "Update the Mojulo solo-builder record after v2.1.0 uses field/ledger evidence to add shell-agent orientation while the unresolved resource remains an independent cold-start user."
tags: [ai-epistemic-worlds, solo-builder, observatory]
---

Run mode: `discovery`.

The merged `solo-builder-triage`, `solo-builder`, `ai-epistemic-intervention` and `ai-epistemic-convergence` contracts were read from current GitHub state before selection or contact. No observatory PR was open. A durable GH-Archive queue refresh could not be completed in this execution runtime, so criteria version `2026-09-21` remains authoritative, `times_sampled` remains 1 and no recurrence or admission-band value was manufactured. The pass therefore used the documented fallback to an already investigated card with material new evidence.

The longitudinal review of `zombico/mojulo` found a material v2.1.0 change. Mojulo's own tool ledger showed that shell-only `npx mojulo call` sessions almost never reached the routing/orientation path that MCP clients receive automatically at initialization. v2.1.0 adds `mojulo orient` as the CLI analogue of MCP initialization, translates MCP call grammar for shell agents, documents it as the first step for agent boxes without an MCP client, and records actual mint/export runs across persistent desktop installs plus ephemeral Claude, ChatGPT/Codex, Grok and Meta Muse boxes. The release intentionally stops short of deeper surface-aware grammar changes pending evidence. This is strong evidence of internal onboarding/host learning, not independent product reception.

The blocker remains `missing_resource: user`, subtype `cold-start-tester`. Accumulated primary-repository reception was refreshed at 3 stars, 0 forks, 3 watchers, 0 subscribers, 0 externally authored issues and 0 externally authored pull requests; Discussions are enabled but accumulated discussion participation and distinct external contributors remain not measured. Full relevant-corpus comparison keeps the strongest bridge `plausible`: GodOS/Scrolls/FlameGrid and EpiLogos are possible world/simulation consumers without a concrete public Mojulo-shaped task; Spark is already blocked on real-device field validation and awaiting response to its observatory issue; Medieval Economy needs methodology review; ISU Survivor needs benchmark instrumentation. No concrete user/resource was found, no convergence was created, and no public contact was made. Mojulo's account-level initial-touch gate remains unused.
