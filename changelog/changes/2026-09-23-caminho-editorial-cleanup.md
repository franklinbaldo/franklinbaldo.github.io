---
type: changelog
date: 2026-09-23
description: Clean Caminho PT/EN editorial residue and mark its Hrönir tier for re-evaluation.
tags: [blog, editorial, hronir, maintenance]
---

# Clean Caminho editorial residue

- Removes the visible `n{/* hronir auto edit */}` process artifact from both published language variants.
- Normalizes the malformed `sunoStyle` block without changing the intended style description.
- Recasts the Guimarães Rosa/Laozi bridge as an explicit interpretive resonance rather than an unsupported historical claim.
- Replaces the categorical `identical` philosophical comparison with `resonates with` / `ressoa com` while keeping PT/EN semantics aligned under `music-caminho`.
- Leaves the existing B/A Hrönir tier as a documented pre-fix baseline and intentionally keeps its `reviewed_revision` stale so the cleaned selection is re-evaluated instead of silently inheriting the old assessment.
- Resolves the safe editorial subset tracked by #2123 without rolling back the selected version.
