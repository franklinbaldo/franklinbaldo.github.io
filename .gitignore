# Keep private agent state only
agent/
.openclaw/

# Secrets
*.env
.env.*
secrets.*
credentials.*

# Runtime lock files

# Session transcripts (SENSITIVE DATA - keep local only)
sessions/

# NPM lock files (updates recorrentes locais)
**/package-lock.json

# Build artifacts (gerados, não versionados)
workspace/.astro/
workspace/dist/
workspace/node_modules/
workspace/backup-posts-locais/

# Workspace duplicado (acidental)
workspace/workspace/
